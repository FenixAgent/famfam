import "./style.css";

export default function loading() {
  const loadingTest = (
    <div className="loadingFrame">
      <h1 className="loadMsg">Loading...</h1>
    </div>
  );

  return loadingTest;
}
